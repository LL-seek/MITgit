# MIT Cheetah DRV8323：STM32Cube + HAL + C 迁移实施手册

> 源项目：MIT_Cheetah_DRV8323_5636  
> 目标平台：STM32F446RET6 + STM32CubeMX/CubeIDE + STM32 HAL + C  
> 文档用途：作为迁移实施顺序、阶段验收、行为追踪和测试证据的唯一主清单  
> 文档状态：初版，开始迁移前完成阶段 0 的复核后冻结基线  
> 最后更新：2026-08-28

---

## 1. 文档目标

本手册不是简单的“旧文件改成 C 文件”清单，而是按以下逻辑组织迁移：

1. 按依赖顺序：上一小节的输出必须能够作为下一小节的输入。
2. 按风险顺序：先无功率、再低压限流、最后闭环和满量程。
3. 按可验证边界：每个小节都必须能够单独编译、观测或测试。
4. 按行为兼容：旧协议、旧参数、状态转换和故障行为必须逐项追踪。
5. 按实时边界：40 kHz 快速路径不得混入延时、打印、Flash 擦写和长循环。
6. 按阶段门推进：阶段验收未通过，不进入依赖该阶段的带电测试。

最终迁移完成的定义不是“新工程可以编译”，而是：

- 新工程完全使用 Cube/HAL/C，不再依赖 mbed 和 C++ 运行时；
- 原项目当前实际使用的硬件与产品行为均已迁移或明确废弃；
- 40 kHz 电流控制链的输入、计算、输出和时序经过验证；
- CAN、参数、标定、故障和启停行为均可复现；
- 参数扇区受链接器保护，保存过程不会在电机运行时擦写 Flash；
- 所有高风险测试均有测试条件、测量结果和结论记录。

---

## 2. 使用方法

### 2.1 状态标记

- [ ] 未开始
- [x] 已完成并有证据
- [ ] 阻塞：在条目下写明阻塞原因
- [ ] 不适用：必须写明批准原因，不能直接删除条目

### 2.2 每个小节的执行闭环

每一小节都按以下顺序执行：

    确认前置条件
        ↓
    实现最小功能
        ↓
    编译与静态检查
        ↓
    台架或主机验证
        ↓
    记录测量证据
        ↓
    勾选完成

任何小节只有同时满足以下条件才允许勾选：

- 接口已经明确；
- 正常路径已经验证；
- 至少一种失败路径已经验证；
- 没有把阻塞操作引入 40 kHz ISR；
- 结果记录在本文件的“测试证据记录”或关联文件中；
- 对后续模块的输出已稳定，不再依赖临时全局变量。

### 2.3 阶段门规则

1. 阶段 0 和阶段 1 是所有硬件迁移的共同前置。
2. 阶段 2 可以与阶段 3、4、5 的底层工作并行设计，但接口冻结必须早于控制集成。
3. 阶段 3、4、5 必须分别完成独立测试后，才能进入阶段 6 的带电闭环测试。
4. 阶段 7 和阶段 8 可以提前开发，但只能通过稳定接口接入阶段 9。
5. 阶段 9 是系统编排层，不允许重新绕过前面模块的安全和错误接口。
6. 任何阶段出现硬件过流、异常发热、角度跳变或控制超周期，立即停止后续带电步骤并回退定位。

### 2.4 推荐证据

可接受的完成证据包括：

- 编译日志；
- 单元测试输出；
- 示波器截图；
- 逻辑分析仪 SPI/CAN 解码；
- UART 诊断日志；
- 参数擦写与回读结果；
- 旧工程与新工程相同输入下的数值对比；
- DWT 周期计数或 GPIO 脉冲测得的 WCET；
- 低压、限流条件下的电流和角度波形。

---

## 3. 总体依赖关系

### 3.1 主依赖链

    0 基线与合同
        ↓
    1 Cube 安全骨架
        ├───────────────┬───────────────┬───────────────┐
        ↓               ↓               ↓               ↓
    2 公共纯 C       3 DRV8323       4 PWM/ADC       5 编码器
        └───────────────┴───────┬───────┴───────────────┘
                                ↓
                            6 控制
                                ↓
                 ┌──────────────┼──────────────┐
                 ↓              ↓              ↓
              7 通信         8 参数         前述设备状态
                 └──────────────┴──────────────┘
                                ↓
                         9 应用与标定
                                ↓
                           最终整机回归

### 3.2 四条必须闭合的主线

硬件数据线：

    时钟/GPIO → SPI/ADC/TIM → DRV/编码器 → 电流/角度/PWM → FOC

产品行为线：

    CAN/UART 命令 → 命令快照 → 应用状态 → FOC 设定值 → 反馈帧

参数生命周期线：

    旧格式 → 新参数结构 → 启动加载 → 控制使用 → 标定更新 → 安全保存

安全线：

    上电安全态 → 数据有效性 → 控制限幅 → 硬件故障 → 安全关断 → FAULT → 受控恢复

四条线全部闭合后，才允许把迁移标记为完成。

---

## 4. 已识别的项目硬件与行为合同

以下内容来自当前旧项目和原理图分析。阶段 0 必须用实物、原理图和旧工程再次复核，复核后把“待确认”改为“已确认”。

### 4.1 MCU、时钟和实时周期

| 项目 | 当前识别值 | 迁移要求 |
|---|---:|---|
| MCU | STM32F446RET6/RETx | Cube 工程选择完全一致的器件 |
| 系统时钟 | 180 MHz | 用实际 HSE 配置复核 |
| APB1 | 45 MHz | 复核 SPI3、CAN 和 USART2 时钟 |
| APB2 | 90 MHz | 复核 TIM1、SPI1 和 ADC 时钟 |
| 控制周期 | 25 us | 对应 40 kHz |
| 当前快速 ISR | TIM1_UP_TIM10_IRQHandler | 新工程必须明确唯一所有者 |
| PWM ARR | 2250，即 0x8CA | 中心对齐下复核实际频率 |
| TIM1 PSC | 0 | 保持或记录有意变更 |
| TIM1 RCR | 1 | 复核更新中断频率 |

### 4.2 引脚与外设

| 功能 | MCU资源 | 引脚 | 当前要求 |
|---|---|---|---|
| U 相 PWM | TIM1_CH3 | PA10 | 与算法 duty_u 映射一致 |
| V 相 PWM | TIM1_CH2 | PA9 | 与算法 duty_v 映射一致 |
| W 相 PWM | TIM1_CH1 | PA8 | 与算法 duty_w 映射一致 |
| DRV ENABLE | GPIO | PA11 | 上电默认关闭 |
| LED | GPIO | PC5 | 只用于状态，不影响实时控制 |
| U/V 电流之一 | ADC1_CH10 | PC0 | 实物确认通道与相名 |
| U/V 电流之二 | ADC2_CH11 | PC1 | 实物确认通道与相名 |
| 母线电压 | ADC3_CH0 | PA0 | 复核分压比例 |
| DRV SPI SCK | SPI1 | PA5 | 16 位、Mode 1 |
| DRV SPI MISO | SPI1 | PA6 | 16 位、Mode 1 |
| DRV SPI MOSI | SPI1 | PA7 | 16 位、Mode 1 |
| DRV SPI CS | GPIO | PA4 | 空闲高 |
| 编码器 SPI SCK | SPI3 | PC10 | 16 位、Mode 1 |
| 编码器 SPI MISO | SPI3 | PC11 | 两颗编码器共用 |
| 编码器 SPI MOSI | SPI3 | PC12 | 两颗编码器共用 |
| 编码器 1 CS | GPIO | PA15 | 空闲高 |
| 编码器 2 CS | GPIO | PD2 | 空闲高 |
| CAN RX | CAN1 | PB8 | 1 Mbps |
| CAN TX | CAN1 | PB9 | 1 Mbps |
| 调试串口 TX | USART2 | PA2 | 921600 baud |
| 调试串口 RX | USART2 | PA3 | 921600 baud |

### 4.3 推荐外设初值

这些值用于建立迁移基线，最终以 Cube 生成值、示波器和逻辑分析仪测量为准。

| 外设 | 推荐初值 | 验证要求 |
|---|---|---|
| TIM1 | Center-aligned、PWM Mode 2、PSC=0、ARR=2250、RCR=1 | 测得三相频率、中心对齐和更新频率 |
| ADC1/2/3 | 三重规则同步模式 | 验证三路样本属于同一控制周期 |
| ADC 时钟 | 建议 PCLK2/4，即 22.5 MHz | 不复制旧代码疑似 45 MHz 的超规格配置 |
| SPI1 | 16 位、CPOL=0、CPHA=1、预分频 256 | 实测接近 351.5625 kHz |
| SPI3 | 16 位、CPOL=0、CPHA=1、预分频 2 | 实测约 22.5 MHz，并验证器件时序 |
| CAN1 | 1 Mbps | 用分析仪确认采样点和稳定性 |
| USART2 | 921600、8N1 | 压力测试不丢失控制周期 |

### 4.4 Flash 规划

| 区域 | 地址范围 | 用途 |
|---|---|---|
| 程序区 | 0x08000000～0x0803FFFF | 256 KiB，链接器必须限制 |
| 参数 A | Sector 6，0x08040000～0x0805FFFF | 推荐有效记录 A |
| 参数 B | Sector 7，0x08060000～0x0807FFFF | 推荐有效记录 B |

禁止事项：

- 链接脚本不得允许程序增长到 0x08040000 以后；
- 电机运行或 40 kHz 控制有效期间不得擦除或写入 Flash；
- 不允许只凭“写函数返回成功”判断保存成功，必须回读和 CRC 验证。

### 4.5 快速路径硬约束

40 kHz ISR 中允许：

- 获取已完成的 ADC 快照；
- 获取编码器快照或执行已经测过时序的固定长度事务；
- 角度、电流和 FOC 固定时间计算；
- 写 TIM1 CCR；
- 更新固定大小的计数器和状态快照。

40 kHz ISR 中禁止：

- HAL_Delay 或毫秒级等待；
- printf、浮点格式化和阻塞 UART；
- Flash 擦写；
- malloc/new/delete/free；
- 标定长循环；
- 菜单交互；
- 等待 CAN 邮箱空闲；
- 无上界重试；
- 任何执行时间随输入规模增长的操作。

---

## 5. 目标工程结构

推荐在源项目旁创建新工程目录 MIT_Cheetah_Cube_HAL_C，旧项目保持只读参考。新工程建议结构：

    MIT_Cheetah_Cube_HAL_C/
    ├─ Core/
    │  ├─ Inc/                  Cube 生成
    │  └─ Src/                  Cube 生成和 USER CODE 桥接
    ├─ Drivers/                 STM32 HAL/CMSIS
    ├─ User/
    │  ├─ BSP/                  板级引脚、时间、紧急关断
    │  ├─ Device/               DRV8323、AS5047
    │  ├─ MotorHW/              PWM、ADC、采样快照
    │  ├─ Control/              FOC、变换、PI、SVM
    │  ├─ Protocol/             MIT CAN、CLI、消息邮箱
    │  ├─ Storage/              参数结构、Flash、CRC、兼容导入
    │  ├─ Calibration/          非阻塞标定状态机
    │  ├─ App/                  应用状态机、调度、故障管理
    │  └─ Common/               类型、数学、通用工具
    └─ Tests/
       ├─ Host/                 可在 PC 运行的纯 C 测试
       └─ Target/               板级诊断和台架测试

层级规则：

1. Common 不依赖 HAL。
2. Control 不直接访问 HAL、GPIO、Flash、CAN 和 UART。
3. Device 可以依赖 HAL 句柄，但不能控制应用状态。
4. MotorHW 负责把 ADC/PWM 转换成明确的物理接口。
5. Protocol 只产生完整命令或事件，不直接开关 PWM。
6. Storage 不在中断中工作。
7. App 是唯一允许组合多个模块并改变系统状态的层。
8. 中断函数只采样、投递、计算或置事件，不执行长业务流程。

---

## 6. 阶段状态总表

| 阶段 | 名称 | 状态 | 开始日期 | 完成日期 | 负责人 | 验收证据 |
|---:|---|---|---|---|---|---|
| 0 | 基线 | 未开始 |  |  |  |  |
| 1 | Cube 骨架 | 未开始 |  |  |  |  |
| 2 | 公共纯 C | 未开始 |  |  |  |  |
| 3 | DRV8323 | 未开始 |  |  |  |  |
| 4 | PWM/ADC | 未开始 |  |  |  |  |
| 5 | 编码器 | 未开始 |  |  |  |  |
| 6 | 控制 | 未开始 |  |  |  |  |
| 7 | 通信 | 未开始 |  |  |  |  |
| 8 | 参数 | 未开始 |  |  |  |  |
| 9 | 应用与标定 | 未开始 |  |  |  |  |

---

# 7. 分阶段实施清单

## 阶段 0：建立可追踪基线

目标：先定义旧项目“实际有什么、如何工作、什么算兼容”，再开始新工程。

输入：当前 Keil/mbed C++ 工程、原理图、DRV8323 数据手册、编码器资料、实物板。  
输出：冻结源码、可复现构建、资源合同、协议合同、时序基线、缺陷决策表。

- [ ] **0.1 冻结源码**
  - 实施：复制或标记当前源项目版本；记录文件清单、日期、编译器版本和工程文件。
  - 完成：旧源码后续只作为参考；任何旧源码变化都有变更记录。

- [ ] **0.2 恢复旧工程编译**
  - 实施：先记录再修复 main.cpp 当前全角分号等阻断构建的问题；保存完整编译日志和 map 文件。
  - 完成：旧工程能够重复编译；记录代码大小、RAM、入口和中断符号。

- [ ] **0.3 文件分类**
  - 实施：给每个旧文件标记“迁移、HAL替代、吸收、暂缓、废弃、未使用待确认”。
  - 完成：源码追踪表中没有“未分类”的文件和目录。

- [ ] **0.4 硬件资源表**
  - 实施：逐项复核第 4 节中的 MCU、引脚、外设、通道、片选、使能、Flash 地址。
  - 完成：资源表与原理图、实物和旧代码三方一致；冲突有决策记录。

- [ ] **0.5 协议合同**
  - 实施：冻结普通 MIT 命令、反馈帧、特殊帧、CAN ID、主站 ID 和超时单位。
  - 完成：为每种帧保存至少一个十六进制输入和预期结果。

- [ ] **0.6 时序基线**
  - 实施：记录 PWM、TIM1 Update、ADC、SPI1、SPI3、CAN 和快速 ISR 的实际时序。
  - 完成：至少有示波器/逻辑分析仪或旧工程寄存器推导结果；不确定项明确标记。

- [ ] **0.7 缺陷决策表**
  - 实施：逐项判断旧代码疑点是“兼容、修复、延后或需要实测”。
  - 完成：本文第 9.2 节所有已知疑点都有负责人、结论和验证方式。

### 阶段 0 验收门

- [ ] 旧工程能重复构建。
- [ ] 所有源文件均已分类。
- [ ] 引脚、外设和 Flash 资源无未解决冲突。
- [ ] CAN 和参数兼容合同已冻结。
- [ ] 关键时序有基线。
- [ ] 旧代码疑点不会在迁移中被无意复制或无记录改变。

---

## 阶段 1：Cube 安全骨架

目标：建立一个能稳定启动、默认不驱动功率级、能诊断并能统一关断的新 C 工程。

输入：阶段 0 的硬件资源合同。  
输出：Cube 工程、正确时钟、安全 GPIO、时间基准、基础 UART、紧急关断、受保护的链接布局。

- [ ] **1.1 CubeMX 工程**
  - 实施：创建 STM32F446RET6 工程；选择纯 C；保存 .ioc；确定 CubeIDE 或目标工具链版本。
  - 完成：空工程无警告构建并下载运行；启动文件和芯片宏正确。

- [ ] **1.2 系统时钟**
  - 实施：按实际 HSE 配置 180 MHz SYSCLK、APB1 45 MHz、APB2 90 MHz；检查 Flash latency 和电源档位。
  - 完成：SystemCoreClock、MCO或定时测量与配置一致。

- [ ] **1.3 安全 GPIO**
  - 实施：在配置输出模式前先写安全电平；PA11 保持 DRV 禁用；所有 CS 空闲高；PWM未接管前保持安全。
  - 完成：复位、下载、看门狗复位和 Error_Handler 时功率级均不会误使能。

- [ ] **1.4 微秒时间基准**
  - 实施：提供无长期溢出误用的微秒计时接口；优先使用 DWT 或专用定时器，不占用控制定时器。
  - 完成：可测量 SPI间隔、启动等待和 WCET；不在 ISR 中提供阻塞延时。

- [ ] **1.5 调试 UART**
  - 实施：配置 USART2 PA2/PA3、921600；先实现最小非阻塞或有界调试输出。
  - 完成：启动信息和错误码可读；持续输出不会改变 40 kHz 时基。

- [ ] **1.6 Error_Handler**
  - 实施：建立 BSP_EmergencyStop；关闭 TIM1 输出、拉低 ENABLE、禁止后续转矩命令并保存错误码。
  - 完成：人为触发 HAL 错误时，示波器确认 PWM 和 ENABLE 进入安全态。

- [ ] **1.7 链接器分区**
  - 实施：程序 Flash 限定在 0x08000000～0x0803FFFF；为 Sector 6/7 参数区保留地址；生成 map 检查。
  - 完成：链接器无法把代码或常量放入参数区；有静态断言或构建后检查。

- [ ] **1.8 代码生成规范**
  - 实施：确定 Cube 生成文件只在 USER CODE 区接桥；业务模块全部放 User；记录重新生成步骤。
  - 完成：重新生成 .ioc 后业务代码不丢失，工程仍可构建。

### 阶段 1 验收门

- [ ] 新工程连续冷启动稳定。
- [ ] 时钟实测正确。
- [ ] 上电、复位和错误路径均保持驱动器关闭。
- [ ] UART 能输出基础诊断。
- [ ] 微秒计时可用于后续测量。
- [ ] 程序区和参数区不会重叠。
- [ ] 尚未执行任何让电机产生转矩的测试。

---

## 阶段 2：公共纯 C 层

目标：先迁移可脱离硬件验证的类型、数学、变换和协议函数。

输入：阶段 0 的行为合同和旧算法。  
输出：无 HAL 依赖的 Common、Control 数学基础和 Protocol 编解码函数。

- [ ] **2.1 数据结构**
  - 实施：列出命令、反馈、ADC快照、位置快照、FOC上下文、故障和参数的数据模型。
  - 完成：模块之间通过结构体和明确接口交换数据，不直接共享内部全局变量。

- [ ] **2.2 固定宽度类型**
  - 实施：使用 stdint.h、stdbool.h、stddef.h；协议和Flash字段禁止依赖 int 的平台宽度。
  - 完成：协议结构和持久化结构的大小有静态断言。

- [ ] **2.3 配置分层**
  - 实施：拆分板级常量、电机常量、可持久化参数和运行时状态；单位写进名称或注释。
  - 完成：算法中不再散落魔法数字；Flash参数不会直接覆盖硬件常量。

- [ ] **2.4 数学函数**
  - 实施：迁移限幅、映射、角度归一化等；使用 fabsf、sqrtf；移除对标准 fminf/fmaxf/roundf 的重定义。
  - 完成：边界、NaN策略、正负零和角度跨界均有测试。

- [ ] **2.5 三角 LUT**
  - 实施：迁移或重建快速正余弦；定义输入范围、插值方式、误差和越界行为。
  - 完成：与标准 sinf/cosf 对比最大误差有记录，执行时间满足控制预算。

- [ ] **2.6 DQ/ABC/SVM**
  - 实施：把 Clarke、Park、逆Park和SVM写成无状态纯函数；固定相序和符号约定。
  - 完成：正反变换回环、典型角度和扇区边界测试通过。

- [ ] **2.7 MIT 协议编解码**
  - 实施：把8字节控制帧解码、6字节反馈编码及浮点量化写成纯函数。
  - 完成：旧工程样例、最小值、最大值、中值和越界帧测试通过。

- [ ] **2.8 主机单元测试**
  - 实施：为 Common、变换、SVM、协议和角度函数建立可重复运行的主机测试。
  - 完成：一次命令可运行全部测试；失败返回非零；结果可以保存。

### 阶段 2 验收门

- [ ] Common 和纯算法模块不包含 HAL 头文件。
- [ ] C 编译器在高警告级别下无新增警告。
- [ ] 数学和协议回归测试通过。
- [ ] 相序、角度正方向、单位和限幅规则已写成接口合同。

---

## 阶段 3：DRV8323

目标：在不产生转矩的条件下完成驱动芯片通信、配置、校准、使能和故障接口。

输入：阶段 1 的 SPI1/GPIO基础和阶段 0 的硬件合同。  
输出：可独立测试的 DRV8323 C 驱动和安全控制接口。

- [ ] **3.1 SPI1**
  - 实施：PA5/PA6/PA7，16位，Mode 1，初始预分频256；CS PA4 空闲高。
  - 完成：逻辑分析仪确认时钟、位宽、模式、频率和片选时序。

- [ ] **3.2 事务层**
  - 实施：实现固定长度 Transfer16；所有超时有上界；事务开始和结束统一管理CS。
  - 完成：成功、HAL超时和总线错误路径均可复现；失败不会把CS留低。

- [ ] **3.3 寄存器层**
  - 实施：统一实现地址、读写位和数据掩码；禁止为每个寄存器复制命令拼接代码。
  - 完成：已知寄存器可读；写后回读一致；专门复核旧 FSR2 命令疑点。

- [ ] **3.4 配置层**
  - 实施：用命名字段配置CSA增益、过流、栅极驱动、PWM模式和故障策略。
  - 完成：启动后回读值与期望配置逐项一致；保留原始寄存器快照。

- [ ] **3.5 启动流程**
  - 实施：定义上电、ENABLE、等待SPI就绪、清故障、配置、回读、自检的顺序。
  - 完成：满足器件ENABLE到SPI就绪等待要求；启动失败保持COAST。

- [ ] **3.6 CSA 校准**
  - 实施：在功率级无电流和安全状态下执行CSA校准；定义超时和结束条件。
  - 完成：重复校准结果稳定；校准过程不会输出PWM转矩。

- [ ] **3.7 ENABLE/COAST**
  - 实施：提供 Enable、Disable/Coast、IsReady 接口；禁止上层直接写 PA11。
  - 完成：任何故障路径都能从模块接口立即进入COAST；示波器验证电平。

- [ ] **3.8 故障解析**
  - 实施：读取FSR1/FSR2，保存原始值，解析为结构化故障位和严重等级。
  - 完成：断线、模拟故障或已知故障条件能被识别并传递给App。

- [ ] **3.9 驱动测试**
  - 实施：建立不使电机转动的寄存器读写、启动、关断、错误注入和重复上电测试。
  - 完成：连续循环无通信错误，异常后保持安全，并有测试日志。

### 阶段 3 验收门

- [ ] SPI1 波形符合要求。
- [ ] 所有关键寄存器可写后回读。
- [ ] ENABLE 时序满足数据手册要求。
- [ ] CSA 校准可重复。
- [ ] 故障可结构化上报。
- [ ] 任何异常均能可靠进入 COAST。

---

## 阶段 4：PWM 和 ADC

目标：建立40 kHz确定性时基、三相安全PWM和可信的同步电流/母线采样。

输入：阶段 1 安全骨架，阶段 3 的驱动关断接口。  
输出：MotorHW PWM接口、ADC快照、电流零偏、比例换算和WCET基线。

- [ ] **4.1 TIM1 配置**
  - 实施：中心对齐、PSC=0、ARR=2250、RCR=1、PWM Mode 2；配置Update中断优先级。
  - 完成：实测PWM和Update频率正确，三通道对齐，无意外窄脉冲。

- [ ] **4.2 U/V/W 映射**
  - 实施：固定 U→CH3/PA10、V→CH2/PA9、W→CH1/PA8；从逻辑名称到CCR只有一个映射点。
  - 完成：逐通道测试与原理图一致，测试记录包含三路波形。

- [ ] **4.3 安全 duty**
  - 实施：定义零转矩、安全中点、最小/最大占空比和非法值处理；复核旧复位值25%/50%疑点。
  - 完成：NaN、Inf、越界命令不会产生危险CCR；决策写入缺陷表。

- [ ] **4.4 PWM 启停**
  - 实施：Start、Stop、EmergencyStop统一控制计数器、通道输出、CCR和DRV状态。
  - 完成：多次启停无残余脉冲；Stop和EmergencyStop行为有明确区别。

- [ ] **4.5 ADC1/2/3**
  - 实施：配置PC0/ADC1_CH10、PC1/ADC2_CH11、PA0/ADC3_CH0；ADC时钟建议22.5 MHz。
  - 完成：三路静态原始值合理；通道注释和实际映射一致。

- [ ] **4.6 同步触发**
  - 实施：确定兼容方案或硬件触发方案；明确相对于中心对齐PWM的采样点和转换完成时刻。
  - 完成：三路数据来自同一控制周期；采样不位于开关尖峰区域；无隐式“靠SPI等待ADC”的假设。

- [ ] **4.7 ADC 快照**
  - 实施：一次性读取三路结果并附带周期号、有效位；控制算法只接收完整快照。
  - 完成：不存在一半新值、一半旧值；快照接口不暴露ADC寄存器。

- [ ] **4.8 ADC 错误**
  - 实施：检测未完成、超时、溢出、不同步和非法范围；定义连续错误阈值。
  - 完成：错误样本不会进入FOC；持续错误能请求FAULT。

- [ ] **4.9 电流零偏**
  - 实施：在PWM关闭且确认无电流时采样足够数量；计算均值、波动和温漂基线。
  - 完成：零偏重复性有记录；运行中禁止在有转矩时重新学习零偏。

- [ ] **4.10 比例校验**
  - 实施：依据ADC参考、电阻、CSA增益和分压把码值转换成A/V；用外部仪表复核。
  - 完成：正负方向、两相量值和母线电压误差在批准范围内。

- [ ] **4.11 WCET/时序测量**
  - 实施：用DWT或GPIO测量Update ISR入口、采样、计算占位和退出；记录平均、最大和抖动。
  - 完成：绝对最大值小于25 us，推荐控制预算不超过20 us，并留有异常处理余量。

### 阶段 4 验收门

- [ ] TIM1 频率、模式和三相映射正确。
- [ ] 安全启停不会产生异常脉冲。
- [ ] ADC三路同步且有有效性标志。
- [ ] 零电流读数接近0 A。
- [ ] 已知电流和母线电压换算正确。
- [ ] ISR不超25 us。

---

## 阶段 5：双编码器

目标：从SPI3合法帧逐层得到连续、方向正确、可供FOC使用的机械角、电角度和速度。

输入：阶段 1 的SPI3/GPIO基础、阶段 2 的角度数学、旧LUT/参数。  
输出：位置传感器快照和明确的数据有效性。

- [ ] **5.1 SPI3**
  - 实施：PC10/PC11/PC12，16位，Mode 1，初始预分频2；PA15和PD2空闲高。
  - 完成：逻辑分析仪确认两颗器件的时钟、片选和总线无争用。

- [ ] **5.2 单颗事务**
  - 实施：实现AS5047命令、响应流水、奇偶校验、错误位和数据掩码。
  - 完成：单颗静止和转动时连续读取稳定；错误帧不会覆盖有效样本。

- [ ] **5.3 双编码器采样**
  - 实施：按固定顺序选择两个CS；结果带同一控制周期号；记录两次事务耗时。
  - 完成：两个编码器可独立辨识，无串扰，采样总耗时满足40 kHz预算。

- [ ] **5.4 LUT**
  - 实施：把原始角和LUT校正角分开保存；定义索引、插值、范围和无有效LUT时的行为。
  - 完成：固定测试向量与旧项目结果一致；损坏LUT不会造成越界访问。

- [ ] **5.5 单编码器跨圈**
  - 实施：处理0/2π跨越、圈计数、方向和异常大跳变。
  - 完成：连续正反转多圈时角度连续；错误帧不会制造假跨圈。

- [ ] **5.6 双编码器齿差**
  - 实施：复现两个编码器之间的齿差/减速比关系；明确符号和安装方向。
  - 完成：手工可复算典型位置；正反转结果互相一致。

- [ ] **5.7 电角度**
  - 实施：由机械角、极对数、相序和电偏置计算电角度并归一化。
  - 完成：手动转动时电角度方向正确；每机械转电周期数符合极对数。

- [ ] **5.8 速度**
  - 实施：基于固定dt和连续角估算速度；定义滤波、无效样本和饱和策略。
  - 完成：静止噪声、恒速正转、恒速反转和跨零测试通过。

- [ ] **5.9 零位**
  - 实施：区分机械零位、电角度零位和用户零位；定义临时置零与保存零位。
  - 完成：置零后各角度含义明确；不会无意改变原始传感器偏置。

- [ ] **5.10 上电圈数初始化**
  - 实施：迁移双编码器上电圈数判定；改为有界、非阻塞或启动状态步骤。
  - 完成：不得在40 kHz ISR中执行10×100 ms等待；成功和失败均有确定状态。

- [ ] **5.11 备用 ABZ 路径决策**
  - 实施：确认旧PositionSensorEncoder路径是否是产品需求；选择迁移、暂缓或明确废弃。
  - 完成：决策写入追踪表；若迁移则单列TIM2/TIM3/PC4资源和验收计划。

### 阶段 5 验收门

- [ ] 两颗编码器都能稳定获得合法帧。
- [ ] 原始角和校正角均可观察。
- [ ] 单编码器跨圈连续。
- [ ] 双编码器输出方向和比例正确。
- [ ] 电角度与相序约定一致。
- [ ] 速度无周期性跨零尖峰。
- [ ] 编码器链耗时满足实时预算。

---

## 阶段 6：FOC 控制

目标：按信号流恢复从电流/角度/命令到三相PWM的确定性闭环。

输入：阶段 2 数学，阶段 4 电流与PWM，阶段 5 角度与速度。  
输出：无HAL依赖的FOC核心、快速ISR集成和分级带电测试结果。

- [ ] **6.1 控制上下文**
  - 实施：把PI积分、滤波、历史角度、热状态和限幅状态放入显式结构体。
  - 完成：无new/delete和隐藏静态状态；可创建、初始化、复位一个控制器实例。

- [ ] **6.2 输入输出接口**
  - 实施：定义FocInput、FocCommand和FocOutput；输入均为本周期完整快照。
  - 完成：FOC核心不读取ADC、CAN、编码器或TIM全局寄存器。

- [ ] **6.3 电流重构**
  - 实施：把两路测量映射为三相电流；明确Ia/Ib/Ic符号、相序和和为零假设。
  - 完成：静止、已知直流注入和相序互换测试有预期结果。

- [ ] **6.4 DQ 变换**
  - 实施：Clarke/Park使用阶段2函数；角度正方向与编码器和PWM统一。
  - 完成：已知电流矢量在已知角度下得到正确Id/Iq。

- [ ] **6.5 指令滤波**
  - 实施：迁移必要的设定值滤波和斜坡；dt明确为25 us或对应调度周期。
  - 完成：阶跃响应、饱和和复位行为与合同一致。

- [ ] **6.6 扭矩外环**
  - 实施：迁移位置/速度/扭矩到Iq参考的逻辑；确认Kp、Kd、前馈的单位。
  - 完成：零命令产生零参考；边界和符号用离线向量验证。

- [ ] **6.7 电流限制**
  - 实施：实现相电流、dq矢量和参数I_MAX限制；规定限幅优先级和抗积分饱和。
  - 完成：任意命令不能绕过最终电流限制。

- [ ] **6.8 弱磁**
  - 实施：迁移I_FW_MAX和速度相关弱磁策略；无有效母线/速度时禁用。
  - 完成：阈值附近连续，无符号突变；低速不会意外产生负Id。

- [ ] **6.9 电流 PI**
  - 实施：用显式dt和带宽计算增益；实现积分限幅/回算；修复旧代码指针比较和abs/fabsf问题。
  - 完成：离线阶跃和饱和恢复测试通过；参数非法时拒绝ARMING。

- [ ] **6.10 前馈决策**
  - 实施：确认是否保留电阻、电感、反电势和dq解耦前馈；每项都可独立开关。
  - 完成：保留项有参数来源和公式；不保留项有决策记录。

- [ ] **6.11 死区补偿**
  - 实施：定义补偿方向、幅值、低电流死区和启用条件；先形成独立策略。
  - 完成：零电流附近不抖动；补偿不能突破占空比安全边界。

- [ ] **6.12 SVM**
  - 实施：逆Park、母线归一化、SVM、死区策略应用、占空比限幅和U/V/W映射。
  - 完成：六扇区连续；低母线和非法母线输入安全；输出范围始终合法。

- [ ] **6.13 热观测器**
  - 实施：迁移绕组/器件热模型、参数和降额策略；明确运行周期。
  - 完成：常温初值、升温、冷却、传感器异常和限流行为有测试。

- [ ] **6.14 控制复位**
  - 实施：统一清除积分、滤波、历史、参考和输出；REST、FAULT、重新ARMING均调用。
  - 完成：二次启动不继承上次积分或占空比。

- [ ] **6.15 ISR 集成**
  - 实施：固定顺序执行快照、有效性检查、FOC_Step、CCR提交和故障置位。
  - 完成：ISR中无阻塞调用和动态分配；所有分支有确定上界。

- [ ] **6.16 分级上电测试**
  - 实施：依次进行无功率波形、低压限流开环、相序、电角度、低增益电流环、完整电流环和扭矩测试。
  - 完成：每一级都有批准结果后才进入下一级；异常可立即EmergencyStop。

### 阶段 6 验收门

- [ ] 纯算法固定向量测试通过。
- [ ] 电流符号、角度方向和U/V/W映射统一。
- [ ] 所有限幅和抗积分饱和有效。
- [ ] 控制复位无残留状态。
- [ ] ISR最大执行时间小于25 us并有余量。
- [ ] 低压限流条件下闭环稳定。
- [ ] 尚未通过的高功率工况被明确禁止。

---

## 阶段 7：CAN 与 UART 通信

目标：恢复外部命令、特殊控制帧、反馈、超时和调试交互，同时隔离通信与40 kHz控制。

输入：阶段 2 协议函数、阶段 6 控制接口、阶段 9 预定义事件接口。  
输出：CAN命令邮箱、应用事件、反馈队列、UART CLI和通信故障。

- [ ] **7.1 CAN 初始化**
  - 实施：配置CAN1 PB8/PB9和1 Mbps位时序；记录时钟、BS1、BS2、SJW和采样点。
  - 完成：与现有主站双向稳定通信；分析仪无持续错误帧。

- [ ] **7.2 过滤器**
  - 实施：只接收需要的电机ID、广播或维护帧；定义改ID前后的过渡行为。
  - 完成：无关总线流量不会进入协议处理；目标帧不漏收。

- [ ] **7.3 RX ISR**
  - 实施：ISR只取帧、记录时间戳并投递固定大小队列/邮箱；不打印、不保存Flash、不切换PWM。
  - 完成：突发流量下ISR有界；队列满有计数和策略。

- [ ] **7.4 命令邮箱**
  - 实施：使用双缓冲、序号或短临界区发布完整命令；FOC每周期只读取一致快照。
  - 完成：压力测试无多字段撕裂；新旧命令序号可追踪。

- [ ] **7.5 特殊帧**
  - 实施：迁移0xFC进入、0xFD退出、0xFE置零、0xFA改CAN ID、尾字节0x01改主站ID。
  - 完成：特殊帧只产生App事件，不在ISR直接执行长动作或Flash写入。

- [ ] **7.6 普通控制帧**
  - 实施：验证DLC、范围和ID后调用纯解码函数；非法帧不刷新有效命令时间。
  - 完成：正常8字节MIT命令的量化、符号和范围与旧项目一致。

- [ ] **7.7 反馈帧**
  - 实施：从一致状态快照构建6字节反馈；定义发送频率、ID和邮箱拥塞策略。
  - 完成：主站正确解析位置、速度、电流/扭矩；发送失败不阻塞控制。

- [ ] **7.8 CAN timeout**
  - 实施：明确旧超时参数以控制周期计数还是毫秒表示；新内部建议统一为时间并提供兼容换算。
  - 完成：断开CAN后在规定时间进入安全行为；恢复通信不自动绕过ARMING。

- [ ] **7.9 CAN 错误**
  - 实施：处理Error Passive、Bus-Off、FIFO溢出和重启；保存计数和最后错误。
  - 完成：Bus-Off不会让旧命令持续输出转矩；恢复策略经过测试。

- [ ] **7.10 UART RX**
  - 实施：用中断/DMA或短ISR接收；实现有限长度行缓冲和溢出处理。
  - 完成：长行、乱码、无换行和高频输入不会破坏内存或控制时序。

- [ ] **7.11 CLI**
  - 实施：迁移必要菜单为命令解析器；危险命令通过App事件并检查当前状态。
  - 完成：CLI不能在MOTOR状态直接擦写Flash或执行阻塞标定。

- [ ] **7.12 输出节流**
  - 实施：状态输出限制为10～20 Hz或按请求输出；使用快照和有界发送。
  - 完成：持续日志时40 kHz WCET和控制抖动无明显变化。

### 阶段 7 验收门

- [ ] CAN 1 Mbps 稳定。
- [ ] 普通帧和全部特殊帧行为有回归记录。
- [ ] 命令邮箱不会撕裂。
- [ ] 超时和Bus-Off均产生安全结果。
- [ ] UART输入有边界检查。
- [ ] UART输出不会阻塞实时控制。

---

## 阶段 8：参数与 Flash

目标：建立版本化、可校验、可兼容旧格式、可抗掉电且只在安全状态保存的参数系统。

输入：阶段 0 参数合同、阶段 1 Flash分区、控制与编码器参数需求。  
输出：参数结构、旧格式导入、A/B记录、加载和保存状态机。

- [ ] **8.1 参数结构**
  - 实施：按物理含义命名字段并写单位、范围和默认值；禁止保留魔法索引作为新接口。
  - 完成：Control、Encoder、CAN和Calibration只通过MotorParameters读取。

- [ ] **8.2 旧索引映射**
  - 实施：逐项固化旧float/int数组下标、类型和含义；处理保留/空洞下标。
  - 完成：旧参数二进制样本可被确定地解释，映射有单元测试。

- [ ] **8.3 新记录头**
  - 实施：定义magic、version、payload_size、sequence和CRC；固定端序和对齐。
  - 完成：结构大小有静态断言；未来版本能够被拒绝或升级。

- [ ] **8.4 参数校验**
  - 实施：检查有限浮点数、极对数、相序、ID、带宽、限流、角度范围和LUT合理性。
  - 完成：单个损坏字段不会直接进入控制器；错误有明确默认/拒绝策略。

- [ ] **8.5 旧格式导入**
  - 实施：启动时识别旧布局，转换成新结构，校验后请求一次新格式保存。
  - 完成：旧有效数据可升级；随机Flash和半写数据不会被误判为有效参数。

- [ ] **8.6 HAL Flash**
  - 实施：封装解锁、擦除、对齐写入、上锁、回读和错误码；只接收明确绝对地址范围。
  - 完成：越界地址被拒绝；Sector 6/7之外绝不擦写。

- [ ] **8.7 双备份/日志**
  - 实施：使用Sector 6/7 A/B记录或经批准的日志策略；以sequence选择最新完整记录。
  - 完成：写新记录失败时旧记录仍有效；选择算法有单元测试。

- [ ] **8.8 保存状态机**
  - 实施：请求保存→退出MOTOR→COAST→停止快速输出→擦写→回读CRC→完成/失败。
  - 完成：无Flash操作发生在ISR或有效控制期间；保存可超时并安全失败。

- [ ] **8.9 加载顺序**
  - 实施：新格式A/B→旧格式导入→安全默认值；加载后先校验再发布运行参数。
  - 完成：任何启动路径都有确定参数来源和诊断状态。

- [ ] **8.10 掉电测试**
  - 实施：分别在擦除前、擦除中、写头、写载荷和提交阶段模拟掉电/复位。
  - 完成：重启后要么恢复上一有效记录，要么使用已定义安全默认值，绝不使用半条记录。

### 阶段 8 验收门

- [ ] 程序链接区不会覆盖参数扇区。
- [ ] 新参数记录有版本和CRC。
- [ ] 旧参数能够导入。
- [ ] 参数范围校验完整。
- [ ] 保存只在安全状态进行。
- [ ] A/B恢复和掉电测试通过。

---

## 阶段 9：应用、标定与整机集成

目标：用显式状态和事件组合全部模块，恢复启动、运行、退出、故障、置零、标定和保存的产品行为。

输入：阶段 3～8 的稳定接口。  
输出：完整应用状态机、非阻塞标定、分频调度、故障快照和最终回归结果。

- [ ] **9.1 应用状态**
  - 实施：至少定义BOOT、REST、ARMING、MOTOR、SETUP、ENCODER_MONITOR、CALIBRATION、PARAM_SAVE、FAULT。
  - 完成：每个状态都有入口动作、周期动作、退出动作和允许事件。

- [ ] **9.2 应用事件**
  - 实施：定义CAN进入/退出/置零、CLI命令、超时、DRV故障、ADC错误、编码器错误、保存和标定事件。
  - 完成：ISR只发布事件；事件队列溢出有安全策略。

- [ ] **9.3 启动顺序**
  - 实施：安全GPIO→时钟/外设→参数加载→驱动自检→ADC零偏→编码器初始化→REST。
  - 完成：任一步失败均进入FAULT或受限REST，不自动使能功率级。

- [ ] **9.4 REST**
  - 实施：PWM安全、DRV COAST、控制器复位；允许诊断、加载、保存和进入ARMING。
  - 完成：在REST长时间停留无异常脉冲或发热。

- [ ] **9.5 ARMING**
  - 实施：检查参数、母线、电流零偏、编码器、DRV、CAN和故障锁存；按顺序启用。
  - 完成：任何前置失败都不会进入MOTOR；成功进入时无转矩阶跃。

- [ ] **9.6 MOTOR**
  - 实施：允许40 kHz控制接管PWM；监控命令、数据有效性、限幅和故障。
  - 完成：只有MOTOR状态可输出主动转矩；退出路径有界且确定。

- [ ] **9.7 FAULT**
  - 实施：第一时间EmergencyStop并锁存首个故障；区分可恢复和不可恢复故障。
  - 完成：清故障需要满足安全条件；不能因通信恢复而自动重新输出转矩。

- [ ] **9.8 零位保存**
  - 实施：置零事件只在允许状态计算新零位；请求PARAM_SAVE后异步写入。
  - 完成：MOTOR中不擦写Flash；断电重启后零位行为符合合同。

- [ ] **9.9 标定状态机**
  - 实施：定义IDLE、PRECHECK、PHASE_ORDER、FORWARD、REVERSE、BUILD_LUT、VALIDATE、SAVE、DONE、ABORT。
  - 完成：每次调度只推进有限工作；状态可观察、可超时、可取消。

- [ ] **9.10 标定前检查**
  - 实施：检查母线、限流、DRV、ADC、两编码器、参数空间和机械活动范围。
  - 完成：任一检查失败禁止施加标定电流并报告具体原因。

- [ ] **9.11 相序**
  - 实施：低压限流下施加受控矢量，判断PHASE_ORDER和编码器方向。
  - 完成：结果可重复；失败或运动异常立即ABORT并COAST。

- [ ] **9.12 正向扫描**
  - 实施：按固定步长和驻留时间扫描；采集两个编码器、命令角、电流和有效性。
  - 完成：无长阻塞循环；所有样本带索引和质量标志。

- [ ] **9.13 反向扫描**
  - 实施：用相同点位反向采集，以估计回差和平均误差。
  - 完成：正反向数据能够配对；异常点有拒绝规则。

- [ ] **9.14 LUT 计算**
  - 实施：合并正反扫描、去除系统偏置、生成128项LUT并检查连续性。
  - 完成：新LUT离线验证优于未校正结果；越界和坏样本不污染数组。

- [ ] **9.15 标定 RAM**
  - 实施：把旧项目动态分配的约43.5 KiB工作区改为静态、联合体复用或分块处理。
  - 完成：map文件确认RAM预算；无malloc/new；栈不会承载大型数组。

- [ ] **9.16 标定中止**
  - 实施：用户取消、CAN超时、DRV故障、编码器错误、过流和超时均进入统一ABORT。
  - 完成：ABORT立即撤销电流参考、复位控制器并进入COAST。

- [ ] **9.17 调度频率**
  - 实施：40 kHz控制；需要时10 kHz标定步；1 kHz应用；100 Hz故障/温度；10～20 Hz输出。
  - 完成：每个任务只有一个所有者；低频任务不在快速ISR中累积无界工作。

- [ ] **9.18 故障快照**
  - 实施：锁存首个故障时间、状态、DRV寄存器、ADC、角度、命令、Id/Iq、母线和CCR。
  - 完成：后续故障不覆盖首因；快照读取不改变故障状态。

- [ ] **9.19 全系统测试**
  - 实施：执行冷启动、热启动、进入/退出、超时、Bus-Off、传感器错误、DRV故障、参数损坏和重复上电。
  - 完成：系统测试矩阵全部有结论，失败项有缺陷单和复测记录。

- [ ] **9.20 mbed/C++ 清理**
  - 实施：搜索mbed、DigitalOut、SPI、CAN、Ticker、wait、new、delete、class、virtual、namespace及.cpp残留。
  - 完成：目标业务代码为.c/.h；第三方HAL/CMSIS之外没有旧运行时依赖。

- [ ] **9.21 构建与内存验收**
  - 实施：保存Release构建日志、map、Flash/RAM/栈预算和参数区检查结果。
  - 完成：无错误、无未经批准警告、无参数区重叠、无动态分配。

- [ ] **9.22 最终回归**
  - 实施：对照阶段0的文件、协议、参数、状态、时序和缺陷表逐项关闭。
  - 完成：每个旧功能均标记为已迁移、HAL替代、修复、暂缓或批准废弃，不存在“可能漏了”。

### 阶段 9 验收门

- [ ] 所有状态转换可复现。
- [ ] 只有MOTOR状态允许主动转矩。
- [ ] 所有故障路径都能安全关断。
- [ ] 标定不阻塞40 kHz控制且可中止。
- [ ] 参数保存不发生在MOTOR状态。
- [ ] CAN、UART、Flash、控制和标定联合测试通过。
- [ ] 旧工程行为追踪表无未关闭项。
- [ ] 最终构建、内存、时序和实机回归全部通过。

---

# 8. 兼容合同

## 8.1 CAN 特殊帧

| 帧特征 | 旧行为 | 新行为目标 | 状态 | 测试证据 |
|---|---|---|---|---|
| 末字节 0xFC | 进入Motor模式 | 发布ENTER_MOTOR事件，经ARMING检查 | 未验证 |  |
| 末字节 0xFD | 退出Motor模式 | 发布EXIT_MOTOR并立即安全撤销转矩 | 未验证 |  |
| 末字节 0xFE | 设置零位 | 发布ZERO请求，只在允许状态处理 | 未验证 |  |
| 末字节 0xFA | 修改CAN ID | 校验后更新参数并请求安全保存 | 未验证 |  |
| 末字节 0x01 | 修改主站ID | 校验后更新参数并请求安全保存 | 未验证 |  |
| 普通8字节帧 | MIT控制命令 | 纯函数解码后发布完整命令快照 | 未验证 |  |
| 6字节反馈 | 位置/速度/电流等 | 保持量化和字段兼容 | 未验证 |  |

## 8.2 旧参数索引

### 浮点参数

| 旧索引 | 字段 | 单位/含义 | 新字段 | 状态 |
|---:|---|---|---|---|
| 0 | E_OFFSET | 电角度偏置 |  | 未映射 |
| 1 | M_OFFSET | 机械零位偏置 |  | 未映射 |
| 2 | I_BW | 电流环带宽 |  | 未映射 |
| 3 | I_MAX | 最大电流 |  | 未映射 |
| 4 | THETA_MIN | 最小位置 |  | 未映射 |
| 5 | THETA_MAX | 最大位置 |  | 未映射 |
| 6 | I_FW_MAX | 最大弱磁电流 |  | 未映射 |

### 整数参数

| 旧索引 | 字段 | 含义 | 新字段 | 状态 |
|---:|---|---|---|---|
| 0 | PHASE_ORDER | 相序 |  | 未映射 |
| 1 | CAN_ID | 电机CAN ID |  | 未映射 |
| 2 | CAN_MASTER | 主站ID |  | 未映射 |
| 3 | CAN_TIMEOUT | 命令超时，旧值按控制周期计数 |  | 未映射 |
| 4～5 | 待确认 | 保留或未使用 |  | 未确认 |
| 6 | RAW_OFFSET | 编码器1原始偏置 |  | 未映射 |
| 7 | RAW2_OFFSET | 编码器2原始偏置 |  | 未映射 |
| 8～135 | LUT[0～127] | 编码器校正表 |  | 未映射 |

---

# 9. 追踪与决策台账

## 9.1 旧源码覆盖表

迁移过程中不得删除行，只修改目标模块、状态和证据。

| 旧源码/目录 | 当前作用 | 目标模块或处理方式 | 阶段 | 状态 | 证据/备注 |
|---|---|---|---:|---|---|
| main.cpp | 主状态、40 kHz ISR、通信和控制编排 | App + ISR桥接 | 6/7/9 | 未开始 |  |
| hw_setup.cpp/.h | 时钟外设和寄存器初始化 | Cube + BSP + MotorHW | 1/3/4/5 | 未开始 |  |
| structs.h | 数据结构和配置 | Common/App/Control types | 2 | 未开始 |  |
| math_ops.cpp/.h | 数学辅助 | Common math | 2 | 未开始 |  |
| Config/ | 参数和硬件配置 | BSP config + Parameters | 2/8 | 未开始 |  |
| DRV8323/ | DRV8323驱动 | Device/DRV8323 | 3 | 未开始 |  |
| FastMath/ | 快速三角/数学 | Common/Control | 2 | 未开始 |  |
| FastPWM/ | mbed PWM封装 | Cube TIM1 + MotorHW | 4 | HAL替代 |  |
| FOC/ | FOC、PI、SVM、热模型 | Control | 2/6 | 未开始 |  |
| PositionSensor/ | 双AS5047及备用ABZ | Device/Position | 5 | 未开始 |  |
| CAN/ | CAN报文和接口 | Protocol/CAN | 2/7 | 未开始 |  |
| FlashWriter/ | Flash底层 | Storage/Flash | 8 | 未开始 |  |
| PreferenceWriter/ | 参数索引和保存 | Storage/Parameters | 8 | 未开始 |  |
| Calibration/ | 相序和编码器标定 | Calibration | 9 | 未开始 |  |
| Tables/ | 查表数据 | 待引用分析 | 0/2/6 | 未确认 |  |
| mbed-dev/及库 | mbed运行时 | 不迁移，由HAL/CMSIS替代 | 1/9 | 待移除 |  |
| startup_stm32f446xx.s | 旧启动文件 | Cube生成启动文件替代 | 1 | 待替代 |  |
| Keil工程文件 | 旧构建系统 | 保留基线，不进入新工程 | 0 | 参考 |  |

## 9.2 已知问题与迁移决策

| ID | 旧代码现象 | 风险 | 默认处理建议 | 最终决策 | 验证证据 |
|---|---|---|---|---|---|
| D-001 | main.cpp存在全角分号导致构建失败 | 无法建立旧基线 | 记录后修复旧基线 | 待定 |  |
| D-002 | hw_setup中TIM1 CCER使用按位取反后OR | 可能误置多个寄存器位 | 不复制，Cube中重建并测波形 | 待定 |  |
| D-003 | DRV FSR2读命令地址编码疑似错误 | 故障读取错误 | 用通用寄存器函数修复 | 待定 |  |
| D-004 | FOC中dtc小于零疑似比较指针而非指向值 | 死区补偿方向错误 | 修复并加单元测试 | 待定 |  |
| D-005 | FOC使用abs处理float | 浮点截断/结果错误 | 改为fabsf | 待定 |  |
| D-006 | 控制复位duty表达式疑似得到25%而非50% | 启动时异常矢量 | 对照实测后定义安全值 | 待定 |  |
| D-007 | 旧ADC配置疑似45 MHz | 超出官方ADC规格 | 新工程先用22.5 MHz并重标定 | 待定 |  |
| D-008 | 40 kHz ISR内存在菜单、标定、打印和等待 | 严重超周期 | 改为事件和非阻塞状态机 | 待定 |  |
| D-009 | 旧链接布局未保护0x08040000参数区 | 程序覆盖参数 | 程序区限制为前256 KiB | 待定 |  |
| D-010 | 标定存在约43.5 KiB动态分配和大缓冲 | 堆碎片/失败/栈风险 | 静态工作区或分块计算 | 待定 |  |
| D-011 | ADC注释、相名和实际通道存在不一致 | 电流相序错误 | 原理图+注入电流实测确认 | 待定 |  |
| D-012 | SPI/ADC时序依赖两次编码器读取的隐式等待 | ADC可能未完成或采样不确定 | 显式触发和完成协议 | 待定 |  |
| D-013 | math_ops重定义标准数学函数 | 链接/语义冲突 | 使用标准函数并重命名自定义项 | 待定 |  |
| D-014 | ABZ编码器路径当前疑似未使用 | 迁移范围膨胀或遗漏需求 | 产品需求确认后迁移/暂缓 | 待定 |  |

## 9.3 接口冻结表

| 接口 | 生产者 | 消费者 | 版本/日期 | 状态 | 备注 |
|---|---|---|---|---|---|
| AdcSnapshot | MotorHW | Control/App |  | 未冻结 |  |
| PositionSnapshot | Device/Position | Control/App |  | 未冻结 |  |
| FocCommand | Protocol/App | Control |  | 未冻结 |  |
| FocOutput | Control | MotorHW/App |  | 未冻结 |  |
| MotorParameters | Storage/App | Control/Position/Protocol |  | 未冻结 |  |
| DrvFault | Device/DRV8323 | App |  | 未冻结 |  |
| AppEvent | Protocol/Device/ISR | App |  | 未冻结 |  |
| FaultSnapshot | App/Control | Diagnostic |  | 未冻结 |  |

---

# 10. 测试证据记录

每完成一个带电或关键时序小节，在下表增加一行。证据文件建议放入新工程 Tests/Evidence，不要只保留在示波器临时存储中。

| 测试ID | 日期 | 对应小节 | 固件版本 | 供电/限流 | 负载条件 | 测试方法 | 结果 | 证据路径 | 结论 |
|---|---|---|---|---|---|---|---|---|---|
| T-001 |  |  |  |  |  |  |  |  |  |

## 10.1 带电测试前检查

- [ ] 实验电源电压已设置。
- [ ] 电流限制已设置并记录。
- [ ] 急停或断电方式可触达。
- [ ] 电机和负载机械固定。
- [ ] 示波器地线连接安全。
- [ ] ENABLE默认低已实测。
- [ ] PWM在下载和复位期间无异常脉冲。
- [ ] 本次只解锁计划中的最小功能。
- [ ] 串口或故障灯可以显示异常。
- [ ] 测试结束条件和中止条件已明确。

## 10.2 WCET 记录

| 固件版本 | 测量点 | SYSCLK | 平均时间 | 最大时间 | 抖动 | 测试工况 | 是否通过 |
|---|---|---:|---:|---:|---:|---|---|
|  | TIM1 Update ISR | 180 MHz |  |  |  |  |  |

## 10.3 数值回归记录

| 用例 | 旧实现输出 | 新实现输出 | 误差 | 容差 | 结论 |
|---|---:|---:|---:|---:|---|
| Clarke/Park典型向量 |  |  |  |  |  |
| 逆Park/SVM典型向量 |  |  |  |  |  |
| MIT命令最小值 |  |  |  |  |  |
| MIT命令中值 |  |  |  |  |  |
| MIT命令最大值 |  |  |  |  |  |
| 编码器LUT典型点 |  |  |  |  |  |
| 双编码器齿差典型点 |  |  |  |  |  |

---

# 11. 决策记录模板

出现会改变行为、接口、范围、时序或安全策略的决定时，复制以下模板：

## ADR-XXX：决策标题

- 日期：
- 背景：
- 旧行为：
- 可选方案：
- 最终选择：
- 选择原因：
- 兼容性影响：
- 安全影响：
- 实时影响：
- 验证方式：
- 批准人：
- 关联小节：

---

# 12. 最终完成定义

只有以下项目全部勾选，项目才算完成迁移：

## 12.1 功能完整性

- [ ] 阶段0识别的每个源文件都有明确归宿。
- [ ] 所有实际使用的DRV8323功能已迁移。
- [ ] 双编码器、角度、速度和零位行为已迁移。
- [ ] FOC、电流限制、弱磁、热保护和复位行为已迁移。
- [ ] 普通CAN命令、特殊帧和反馈均已迁移。
- [ ] 参数加载、旧格式导入、保存和掉电恢复已迁移。
- [ ] 启动、REST、ARMING、MOTOR、FAULT和标定流程已迁移。
- [ ] ABZ等备用功能已迁移或有批准的范围决策。

## 12.2 安全完整性

- [ ] 上电、复位、下载和Error_Handler均不会误驱动。
- [ ] 只有MOTOR状态允许主动转矩。
- [ ] ADC、编码器、DRV、CAN超时和软件异常都能安全关断。
- [ ] Flash写入只发生在功率输出停止后。
- [ ] 标定可取消、可超时、可故障中止。
- [ ] 所有控制输出都有有限值和范围检查。

## 12.3 实时完整性

- [ ] 40 kHz控制周期实测正确。
- [ ] 最坏执行时间小于25 us并有批准余量。
- [ ] ISR中没有延时、打印、Flash、动态分配和无界循环。
- [ ] CAN/UART压力不会使控制超周期。
- [ ] ADC和编码器样本均有周期一致性和有效性。

## 12.4 构建与维护完整性

- [ ] 目标业务代码全部为C。
- [ ] 无mbed依赖、无C++运行时依赖、无动态内存。
- [ ] Cube重新生成不会覆盖业务代码。
- [ ] Release构建无错误和未经批准警告。
- [ ] map确认Flash、RAM、栈和参数扇区安全。
- [ ] 主机单元测试和目标板测试可重复执行。
- [ ] 本文所有阶段门已经关闭。

## 12.5 最终签署

| 角色 | 姓名 | 日期 | 结论 |
|---|---|---|---|
| 迁移实施 |  |  |  |
| 代码复核 |  |  |  |
| 台架测试 |  |  |  |
| 最终批准 |  |  |  |

---

## 13. 建议的实际开工顺序

第一次执行时，不要直接创建全部模块。建议按以下最小批次推进：

1. 完成 0.1～0.7，冻结合同。
2. 完成 1.1～1.8，只验证安全启动。
3. 完成 2.1～2.8，先让纯 C 测试跑起来。
4. 分别完成 3、4、5 的独立板级测试，期间不接入完整FOC。
5. 完成 6.1～6.15 的离线和无功率验证。
6. 严格按 6.16 分级带电。
7. 接入 CAN/UART，但通信只通过命令邮箱和事件。
8. 接入参数系统，先读和校验，再测试安全保存。
9. 用阶段9状态机把所有模块组合起来。
10. 回到阶段0合同逐项关闭，完成最终回归。

如果某个步骤需要临时数据，应使用明确的测试夹具：

- FOC未接CAN时，使用固定FocCommand；
- 编码器未接Flash时，使用编译期测试LUT；
- App未完成时，设备驱动使用独立诊断程序；
- 任何临时代码都加 TEST_ONLY 标记，并在9.22前清除。

迁移过程的核心原则：

> 先让每个输入可信，再让每个输出安全；先证明单模块正确，再允许系统组合；先通过阶段门，再提高电压、电流和功能复杂度。
